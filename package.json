{
	"name": "python-prepare-action",
	"version": "0.0.0",
	"type": "module",
	"description": "A GitHub Action for setting up useful stuff in Python workflows.",
	"scripts": {
		"build": "tsc",
		"lint": "eslint --max-warnings=0 ./src/",
		"test": "vitest run",
		"bootstrap": "npm install && npm run build && npm prune --production"
	},
	"license": "MIT",
	"dependencies": {
		"@actions/cache": "^6.0.0",
		"@actions/core": "^3.0.0",
		"@actions/exec": "^3.0.0",
		"reflect-metadata": "^0.2.2",
		"source-map-support": "^0.5.21"
	},
	"devDependencies": {
		"@tsconfig/node20": "^20.1.6",
		"@types/source-map-support": "^0.5.10",
		"@types/node": "^24.0.0",
		"eslint": "^10.0.0",
		"github-actions-problem-matcher-typings": "^1.1.0",
		"typescript": "^5.9.2",
		"typescript-eslint": "^8.44.0",
		"vitest": "^4.0.0"
	}
}
